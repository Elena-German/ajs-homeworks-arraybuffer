// eslint-disable-next-line no-unused-vars
import Bowerman from './Bowerman';
// eslint-disable-next-line no-unused-vars
import Magician from './Magician';
// eslint-disable-next-line no-unused-vars
import Daemon from './Daemon';
// eslint-disable-next-line no-unused-vars

/* const player = new Daemon('leo');
console.log(`атака при создании нового игрока - ${player.attack}`); // должно быть 10
player.distance = 2;
console.log(`атака на вторую клетку - ${player.attack}`); // должно быть 9
// player.distance = 1;
player.levelUp();
console.log(`атака после повышения уровня - ${player.attack}`); // должно быть 12
*/
